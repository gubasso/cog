# Workflow stub: plan

Write a short plan for the task described in the run's task file. Write it to `plan.md` inside this node's own directory under the run directory. Write nothing outside that directory.
