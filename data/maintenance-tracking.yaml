# Registry of perishable artifacts that need periodic re-research / revalidation.
# `cog tracking-scan` reports entries whose (last_checked + cadence_days) is in the past.
schema_version: 1
entries:
  - id: models-reference-claude
    path: docs/reference/models-reference-claude.md
    last_checked: 2026-06-25
    cadence_days: 90
    owner: model-effort policy
    why: "Claude model pricing, benchmarks, and available tiers change; the model/effort policy rests on them."
    revalidate_how: "Web-research official Anthropic sources; update figures + Data collected date; re-derive policy if tiers shift."
    references:
      - docs/reference/model-effort-policy.md
      - data/model-effort/claude
      - data/power-grade/matrix
  - id: models-reference-codex
    path: docs/reference/models-reference-codex.md
    last_checked: 2026-06-25
    cadence_days: 90
    owner: model-effort policy
    why: "Codex-selectable GPT models, pricing, and effort multipliers change; the codex policy rests on them."
    revalidate_how: "Web-research official OpenAI/Codex sources; update figures + Data collected date; re-check the -codex API-only caveat."
    references:
      - docs/reference/model-effort-policy.md
      - data/model-effort/codex
      - data/power-grade/matrix
  - id: power-grade-matrix
    path: data/power-grade/matrix
    last_checked: 2026-06-25
    cadence_days: 90
    owner: power-grade policy
    why: "The matrix grades model/effort capability from model benchmarks, pricing, effort support, and policy defaults that can drift."
    revalidate_how: "Refresh docs/reference/models-reference-claude.md and docs/reference/models-reference-codex.md from authoritative sources, preserve NEEDS VERIFICATION for gaps, then update grades, source_refs, and this last_checked date. The downstream data/power-grade/executor-capability powers and routing thresholds re-derive from these grades; re-run cog power-grade executor-validate after any grade change."
    references:
      - docs/reference/model-effort-policy.md
      - docs/decisions/0032-power-grade-model.md
      - data/power-grade/executor-capability
  - id: power-grade-source-allowlist
    path: data/power-grade/source-allowlist
    last_checked: 2026-06-25
    cadence_days: 90
    owner: power-grade policy
    why: "Trusted-source URLs, tiers, and leaderboard domains drift; the matrix sourcing policy rests on them."
    revalidate_how: "Re-check each source URL is live and canonical, re-confirm tier and clears_needs_verification, then update last_checked. Tier 3 entries stay recorded as excluded."
    references:
      - docs/decisions/0033-benchmark-source-tiers.md
      - data/power-grade/matrix
  - id: executor-capability
    path: data/power-grade/executor-capability
    last_checked: 2026-06-30
    cadence_days: 90
    owner: power-grade policy
    why: "Executor pass composition and capability calibration derive from the executor-* skill flows, the model-cells matrix, and the complexity rubric's max score, all of which can drift."
    revalidate_how: "Re-confirm each native executor-* skill's pass list and declared/driven model-effort against passes.yaml, re-check calibration.yaml's complexity max_score against the rubric, then run cog power-grade executor-validate and update last_checked."
    references:
      - docs/decisions/0054-executor-capability-grading.md
      - data/power-grade/matrix
      - skill-refs/plan-rounds/complexity-rubric.md
      - skills/claude/executor-oneshot/SKILL.md
      - skills/claude/executor-vetted/SKILL.md
      - skills/claude/executor-prex/SKILL.md
  - id: vacated-multi-codex-authoring-path
    path: skills/claude/plan-builder-to-queue/SKILL.md
    last_checked: 2026-06-30
    cadence_days: 180
    owner: plan authoring
    why: "The multi-engine plan-writer-multi (Claude) and plan-writer (Codex) authoring surfaces were deleted outright (ADR-0056 / DP6), to be re-created from scratch at the proper time; this note tracks that intentionally-vacated surface so the rebuild is not forgotten."
    revalidate_how: "Decide whether a from-scratch multi-engine / Codex plan-authoring surface is still wanted; if so, author it fresh against the current plan-vault + executor-routing contracts; otherwise extend the cadence."
    references:
      - docs/decisions/0056-plan-round-executor-routing-contract.md
      - skills/claude/plan-builder-to-queue/SKILL.md
  - id: match-telemetry-calibration
    path: data/power-grade/executor-capability
    last_checked: 2026-06-30
    cadence_days: 30
    owner: power-grade policy
    why: "Plan→executor match-outcome telemetry accumulates cross-project; periodically review whether the executor-capability powers / complexity-rubric weights should be refit against observed over/under-powered rounds. Grades are never auto-mutated (ADR-0008)."
    revalidate_how: "Run cog match-telemetry report; review the over-powered / under-powered / needs-review rows; decide whether to refit executor-capability powers or complexity-rubric weights; apply by hand if warranted, then update last_checked. Never auto-applied."
    references:
      - docs/decisions/0058-match-outcome-telemetry-and-calibration-loop.md
      - data/power-grade/executor-capability/passes.yaml
      - data/power-grade/executor-capability/calibration.yaml
      - skill-refs/plan-rounds/complexity-rubric.md
  - id: research-shelf
    path: data/research-shelf/index.jsonl
    last_checked: 2026-06-20
    cadence_days: 90
    owner: research-shelf
    why: "Research-shelf entries capture external best-practice and product behavior that can drift over time."
    revalidate_how: "Run cog tracking-scan, re-research overdue shelf topics from current sources, append refreshed findings with cog research-shelf record, then update this registry entry's last_checked date."
    references:
      - docs/reference/research-shelf/README.md
